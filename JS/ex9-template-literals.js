(function() {

    // The template string uses the back-tick character (above the tab key in the upper-left of the keyboard)

    console.info(`Scenario 1:
    Writing a 
    multi-line
    string!`);

    let iScenarioNum = 2;
    console.info(`Scenario ${iScenarioNum}: String interpolation!`);

})();